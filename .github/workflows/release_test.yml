# SPDX-FileCopyrightText: 2025 Deutsche Telekom AG and others
#
# SPDX-License-Identifier: Apache-2.0

name: Release Test

on:
  push:
    branches: 
      - "generate-main-sbom"

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Pause workflow run
        run: sleep 60s